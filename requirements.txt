# 实时双语字幕 —— Windows 运行依赖
# 用法见 setup_windows.bat / run_windows.bat
#
# Windows CPython 3.11 默认安装统一托管在 ModelScope 的 CPU wheel；
# 其他平台或 Python 版本使用官方版本。
sherpa-onnx @ https://modelscope.cn/models/XHxiehuan/LiveBabel-sherpa-onnx-wheels/resolve/master/wheels/sherpa_onnx-1.12.17-cp311-cp311-win_amd64.whl ; sys_platform == "win32" and python_version == "3.11"
sherpa-onnx==1.13.2 ; sys_platform != "win32" or python_version != "3.11"
numpy
soundfile
# requests 同时用于首次点击朗读时从公开 ModelScope 地址按需下载 ChatTTS 模型。
requests
rich
PySide6
pyaudiowpatch        # Windows 系统声音采集(WASAPI loopback),仅 Windows 可装
keyboard ; platform_system == "Windows"   # 语音输入:全局热键 + 模拟粘贴(仅 Windows)
sounddevice           # TTS 朗读:音频播放(PortAudio,跨平台)

# 离线字幕与实时/会议共用 sherpa-onnx 的 Qwen3-ASR，无额外 ASR 运行时依赖。
